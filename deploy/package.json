{
  "name": "shigotoku-deploy",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "deploy:web": "npm run build && firebase deploy --only hosting:shigotoku-web",
    "deploy:runwith-app": "npm run build && firebase deploy --only hosting:runwith-app",
    "deploy:buzzit-app": "npm run build && firebase deploy --only hosting:buzzit-app",
    "deploy:buzzit-api": "npm run build:buzzit-api && firebase deploy --only functions:buzzit-api,storage",
    "build:buzzit-api": "npm ci --prefix ../buzzit/api && npm run build --prefix ../buzzit/api && node scripts/prepare-buzzit-api.mjs && cp ../buzzit/api/storage.rules storage.rules",
    "deploy:all": "npm run build && npm run build:buzzit-api && firebase deploy --only hosting,functions:buzzit-api,storage"
  },
  "devDependencies": {
    "firebase-tools": "^14.0.0"
  }
}
