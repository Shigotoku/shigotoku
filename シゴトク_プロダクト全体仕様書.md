# シゴトク プロダクト全体仕様書

**株式会社シゴトク（Shigotoku）— 全アプリ・ホームページ・インフラの技術仕様**

| 項目 | 内容 |
|------|------|
| 最終更新 | 2026-05-26 |
| リポジトリ | [github.com/Shigotoku/shigotoku](https://github.com/Shigotoku/shigotoku) |
| 構成 | フォルダ型モノレポ（npm workspaces なし、各サブプロジェクト独立） |
| Node.js | **22.x**（本番・CI 共通） |

---

## 目次

1. [プロダクト一覧](#1-プロダクト一覧)
2. [本番 URL マップ](#2-本番-url-マップ)
3. [クラウド・インフラ](#3-クラウドインフラ)
4. [技術スタック早見表](#4-技術スタック早見表)
5. [ホームページ・ランディングページの作り方](#5-ホームページランディングページの作り方)
6. [Web アプリの作り方](#6-web-アプリの作り方)
7. [BuzzIt API（バックエンド）](#7-buzzit-apiバックエンド)
8. [認証・データベース](#8-認データベース)
9. [課金モデル](#9-課金モデル)
10. [外部サービス連携](#10-外部サービス連携)
11. [ビルド・デプロイ・CI/CD](#11-ビルドデプロイcicd)
12. [環境変数](#12-環境変数)
13. [リポジトリ構成](#13-リポジトリ構成)
14. [関連ドキュメント索引](#14-関連ドキュメント索引)

---

## 1. プロダクト一覧

| プロダクト | 日本語名 | 種別 | フォルダ | 概要 |
|-----------|---------|------|---------|------|
| **Corporate** | シゴトク ホームページ | コーポレートサイト | `corporate-site/` | 会社概要・法務ページ・各プロダクトへの導線 |
| **RunWith LP** | ランウィズ | ランディングページ | `runwith/landing-page/` | 創業支援 SaaS の訴求・料金 |
| **RunWith App** | ランウィズ | Web アプリ（SPA） | `runwith/app/` | 創業ジャーニー・法人設立・資金調達・KPI 等 |
| **BuzzIt LP** | バジット（BuzzIt） | ランディングページ | `buzzit/landing-page/` | 美容サロン向け「経営 OS」の訴求・料金・試算 |
| **BuzzIt App** | バジット（BuzzIt） | Web アプリ（SPA） | `buzzit/app/` | SNS 運用・LINE CRM・分析ダッシュボード |
| **BuzzIt API** | バジット（BuzzIt） | REST API | `buzzit/api/` | AI 生成・投稿スケジュール・外部連携 |
| **Deploy** | （インフラ） | ビルド・デプロイ | `deploy/` | 全プロジェクト統合ビルド・Firebase 設定 |

> **補足:** `runwith/supabase/migrations/` に SQL マイグレーションが残っていますが、**本番は Supabase 未使用**。RunWith のバックエンドは **Firebase（Firestore）** です。

---

## 2. 本番 URL マップ

| URL | 内容 | Hosting サイト |
|-----|------|-----------------|
| https://shigotoku.com/ | コーポレートサイト | `shigotoku-web` |
| https://shigotoku.com/privacy/ | プライバシーポリシー | 同上 |
| https://shigotoku.com/terms/ | 利用規約 | 同上 |
| https://shigotoku.com/tokushoho/ | 特定商取引法 | 同上 |
| https://shigotoku.com/security/ | セキュリティ | 同上 |
| https://shigotoku.com/runwith/ | ランウィズ LP | 同上（サブパス） |
| https://shigotoku.com/runwith/pricing/ | ランウィズ 料金 | 同上 |
| https://shigotoku.com/buzzit/ | BuzzIt LP | 同上（サブパス） |
| https://app.runwith.shigotoku.com | ランウィズ アプリ | `shigotoku-runwith-app` |
| https://app.buzzit.shigotoku.com | BuzzIt アプリ | `shigotoku-buzzit-app` |
| https://app.buzzit.shigotoku.com/api/** | BuzzIt API（Hosting リライト） | Cloud Functions |

Firebase デフォルト URL（カスタムドメインの代替）:

- https://shigotoku-web.web.app
- https://shigotoku-buzzit-app.web.app
- https://shigotoku-runwith-app.web.app

---

## 3. クラウド・インフラ

### 3.1 クラウドプロバイダ

**Google Cloud Platform（GCP）** を基盤とし、主に **Firebase** サービスを利用しています。

| GCP / Firebase サービス | 用途 |
|------------------------|------|
| **Firebase Hosting** | 静的サイト（コーポレート・LP）+ SPA（アプリ） |
| **Cloud Functions v2** | BuzzIt REST API・スケジューラ・投稿ワーカー |
| **Cloud Firestore** | ユーザー設定・KPI・店舗/会社・投稿ジョブ等 |
| **Firebase Authentication** | ログイン（Google / メール / 匿名デモ） |
| **Cloud Storage（Firebase Storage）** | メディアアップロード・素材保存 |
| **Secret Manager** | API キー（Gemini, Ayrshare, Slack 等） |
| **Cloud Scheduler** | 定期実行（Auto Mode・投稿ワーカー） |

**使用していないもの（本番）:** Supabase、AWS、Vercel、Netlify

### 3.2 Firebase プロジェクト分割

セキュリティと Auth 分離のため、**2 つの Firebase プロジェクト**に分かれています。

| Firebase プロジェクト ID | エイリアス | ホストするもの |
|-------------------------|-----------|---------------|
| `shigotoku-prod` | `buzzit` | コーポレート + BuzzIt LP + BuzzIt アプリ + BuzzIt API |
| `shigotoku-runwith-prod` | `runwith` | RunWith アプリのみ |

> BuzzIt と RunWith は **別 Auth テナント**のため、同じメールアドレスでも別アカウントになります。

### 3.3 リージョン

- BuzzIt Cloud Functions: **`asia-northeast1`（東京）**
- Firestore / Storage: プロジェクトデフォルト（東京リージョン想定）

### 3.4 設定ファイル（deploy/）

| ファイル | 役割 |
|---------|------|
| `deploy/firebase.json` | 統合定義（3 Hosting + Functions + ルール） |
| `deploy/firebase.buzzit.json` | BuzzIt プロジェクト用デプロイ |
| `deploy/firebase.runwith.json` | RunWith プロジェクト用デプロイ |
| `deploy/.firebaserc` | プロジェクト ID・Hosting ターゲットマッピング |
| `deploy/firestore.buzzit.rules` | BuzzIt Firestore セキュリティルール |
| `deploy/firestore.runwith.rules` | RunWith Firestore セキュリティルール |
| `deploy/storage.buzzit.rules` | BuzzIt Storage ルール |
| `deploy/storage.runwith.rules` | RunWith Storage ルール |
| `deploy/build.mjs` | 全フロントエンド統合ビルド |
| `deploy/build.config.mjs` | 本番 URL・Firebase 設定・環境変数注入 |

---

## 4. 技術スタック早見表

| レイヤ | コーポレート / LP | Web アプリ | BuzzIt API |
|--------|------------------|-----------|------------|
| **言語** | TypeScript | TypeScript | TypeScript |
| **フレームワーク** | Astro 6 | React 19 | Express 5 |
| **ビルド** | `astro build` | Vite 8 + `tsc` | `tsc` → Node 22 |
| **スタイル** | Tailwind CSS 4 | Tailwind CSS 4 | — |
| **ルーティング** | ファイルベース（Astro pages） | React Router 7 | Express ルート |
| **状態管理** | — | React Context / Zustand（RunWith） | — |
| **認証** | — | Firebase Auth | Bearer トークン検証 |
| **DB** | — | Firestore（クライアント / API 経由） | Firestore（Admin SDK） |
| **ホスティング** | Firebase Hosting（静的） | Firebase Hosting（SPA） | Cloud Functions |

---

## 5. ホームページ・ランディングページの作り方

### 5.1 共通アーキテクチャ

コーポレートサイトおよび各プロダクトの LP は、すべて **Astro** で構築した **静的サイト（SSG）** です。

```
src/
├── pages/          ← URL ごとの .astro ファイル（ルーティング）
├── components/     ← 再利用 UI ブロック（.astro）
├── layouts/        ← 共通レイアウト（ヘッダー・フッター・SEO）
├── lib/            ← ユーティリティ（URL 生成・画像パス等）
└── styles/         ← グローバル CSS（Tailwind）
```

**ビルド:** `npm run build` → `dist/` に HTML/CSS/JS が出力 → Firebase Hosting へ配置

**スタイリング:** Tailwind CSS 4（`@tailwindcss/vite` プラグイン経由）

**SEO:** 各プロジェクトに `Seo.astro` / `Analytics.astro`（GA4）コンポーネント

### 5.2 各サイトの詳細

#### コーポレートサイト（`corporate-site/`）

| 項目 | 内容 |
|------|------|
| 配置 URL | `https://shigotoku.com/`（ルート） |
| Astro `base` | なし（ルート配置） |
| ページ | `/`, `/privacy`, `/terms`, `/tokushoho`, `/security` |
| お問い合わせ | Formspree 連携（プレースホルダー ID） |
| 起動 | `cd corporate-site && npm install && npm run dev` |

#### ランウィズ LP（`runwith/landing-page/`）

| 項目 | 内容 |
|------|------|
| 配置 URL | `https://shigotoku.com/runwith/` |
| Astro `base` | `/runwith/` |
| Astro `site` | `https://shigotoku.com` |
| ページ | `index.astro`（トップ）, `pricing.astro`（料金） |
| 起動 | `cd runwith/landing-page && npm install && npm run dev` |

#### BuzzIt LP（`buzzit/landing-page/`）

| 項目 | 内容 |
|------|------|
| 配置 URL | `https://shigotoku.com/buzzit/` |
| Astro `base` | `/buzzit/` |
| ページ | `index.astro`（単一 LP・セクション構成） |
| 特徴 | LINE 配信コスト試算シミュレーター（`LineCostSimulator.astro`）、Hero コラージュ画像生成スクリプト |
| 起動 | `cd buzzit/landing-page && npm install && npm run dev` |

### 5.3 LP の本番への載せ方

`deploy/build.mjs` が 3 つの Astro プロジェクトを順にビルドし、1 つの Hosting サイトにマージします。

```
corporate-site/dist/     → deploy/dist/web/
runwith/landing-page/dist/ → deploy/dist/web/runwith/
buzzit/landing-page/dist/  → deploy/dist/web/buzzit/
```

加えて `robots.txt` と `sitemap.xml` を自動生成して `dist/web/` に配置します。

---

## 6. Web アプリの作り方

### 6.1 共通アーキテクチャ

RunWith アプリ・BuzzIt アプリは、いずれも **React + Vite** の **SPA（Single Page Application）** です。

```
src/
├── pages/          ← 画面コンポーネント
├── components/     ← 共通 UI
├── layouts/        ← ダッシュボードレイアウト等
├── lib/            ← API クライアント・ユーティリティ
├── store/          ← React Context / Zustand ストア
├── services/       ← Firebase / 外部 API 呼び出し
└── types/          ← TypeScript 型定義
```

**ビルド:** `tsc && vite build`（BuzzIt は `tsc -b`）→ `dist/` → 専用 Firebase Hosting サイトへ

**ルーティング:** React Router 7。Hosting 側で `**` → `/index.html` リライト（SPA フォールバック）

**SEO:** アプリドメインは `X-Robots-Tag: noindex, nofollow`（検索インデックス対象外）

### 6.2 ランウィズ アプリ（`runwith/app/`）

| 項目 | 内容 |
|------|------|
| URL | https://app.runwith.shigotoku.com |
| Firebase プロジェクト | `shigotoku-runwith-prod` |
| 状態管理 | **Zustand**（`persist` 付き） |
| チャート | **recharts** |
| 開発ポート | 5174 |
| デモモード | Firebase 未設定時もローカルストレージで動作 |

**主要機能（ルート一覧）:**

- ダッシュボード、創業ジャーニー（アイデア・市場規模・ペルソナ・BMC 等）
- 法人設立・会社設立・資金調達検索
- ネーミング・商標・コスト試算
- KPI トラッカー、ピッチデック、投資家マッチング
- チーム招待・会社管理（`/team/invite`）
- 設定・管理画面（`/admin`）

**起動:** `cd runwith/app && npm install && npm run dev`

### 6.3 BuzzIt アプリ（`buzzit/app/`）

| 項目 | 内容 |
|------|------|
| URL | https://app.buzzit.shigotoku.com |
| Firebase プロジェクト | `shigotoku-prod` |
| API | デフォルト `/api`（Hosting リライト → Cloud Functions） |
| UI | framer-motion, lucide-react |
| 認証 | Google / メール / **匿名デモ** |

**主要画面（ルート一覧）:**

| パス | 画面 |
|------|------|
| `/login` | ログイン |
| `/dashboard` | 経営コクピット |
| `/magic-creator` | マジック・クリエイター（AI 台本・Repurpose） |
| `/line-crm` | LINE CRM（Lステップ代替） |
| `/analytics` | 分析・売上 |
| `/settings` | 設定（プラン・連携・店舗請求） |
| `/team` | スタッフ管理・招待 |
| `/invite/:token` | 招待承認 |

**起動:** `cd buzzit/app && npm install && npm run dev`

ローカル開発時、Vite が `/api` を Firebase Emulator（`127.0.0.1:5001`）へプロキシします。

---

## 7. BuzzIt API（バックエンド）

BuzzIt のみ、**Cloud Functions v2 上の Express REST API** を持ちます。RunWith はクライアントから Firestore に直接アクセスする構成です。

| 項目 | 内容 |
|------|------|
| フォルダ | `buzzit/api/` |
| ランタイム | Node.js **22** |
| フレームワーク | Express 5 + `firebase-functions` v2 |
| エントリ | `src/index.ts` → `lib/index.js` |
| デプロイ先 | `deploy/functions/buzzit-api/`（`prepare-buzzit-api.mjs` でコピー） |

### 7.1 エクスポート関数

| 関数名 | 役割 |
|--------|------|
| `buzzitApi` | REST API（`/api/**` への Hosting リライト先） |
| `buzzitScheduler` | 定期実行（Auto Mode 等） |
| `buzzitPublishWorker` | 5 分ごとの予約投稿処理 |

### 7.2 主要 API カテゴリ

| カテゴリ | 例 |
|---------|-----|
| 認証 | `POST /v1/auth/bootstrap` |
| ダッシュボード | `GET /v1/dashboard`, `/v1/analytics` |
| 設定 | `GET/PUT /v1/settings` |
| AI | `POST /v1/repurpose`, `/v1/voice-draft` |
| 投稿 | `POST /v1/schedule`, `/v1/scheduled/:id/approve` |
| LINE CRM | `/v1/line/*`（タグ・セグメント・ステップ・配信） |
| 店舗・課金 | `/v1/stores`, `/v1/billing`, 招待 API |
| 外部連携 | Meta OAuth, Slack, トラッキング |
| メトリクス | `POST /v1/metrics/event` |

**起動（ローカル）:** `cd buzzit/api && npm install && npm run serve`

---

## 8. 認証・データベース

### 8.1 認証（Firebase Authentication）

| プロダクト | 方式 |
|-----------|------|
| RunWith | Google OAuth、メール/パスワード、メール検証 |
| BuzzIt | Google OAuth、メール/パスワード、**匿名（デモ）** |

API 認証: BuzzIt API は `Authorization: Bearer {Firebase ID Token}` を検証。

### 8.2 データベース（Cloud Firestore）

#### BuzzIt（`shigotoku-prod`）

```
users/{uid}                    ← プラン・設定・storeIds・activeStoreId
stores/{storeId}               ← 店舗情報
stores/{storeId}/members/{uid} ← スタッフ（owner/manager/staff）
stores/{storeId}/invitations/  ← 招待
buzzit_invitation_tokens/{token}
（サブコレクション: scheduled, posts, metrics, line tags 等）
```

#### RunWith（`shigotoku-runwith-prod`）

```
runwith_users/{uid}
runwith_companies/{companyId}
runwith_companies/{companyId}/members/{uid}
runwith_companies/{companyId}/subscription/current
（各種 journey / 進捗データ）
```

### 8.3 ストレージ（Cloud Storage）

- BuzzIt: メディアアップロード（`uploads/`）、音声素材
- RunWith: 会社スコープのファイル

---

## 9. 課金モデル

### 9.1 BuzzIt — 店舗単位課金

```
月額 = 1店舗目の基本料 + 2店舗目以降 × 基本料 × 0.8（20% OFF）
```

| プラン | 基本料 | 最大店舗 | スタッフ上限 |
|--------|--------|----------|--------------|
| Free | ¥0 | 1 | 1 |
| LINE管理ライト | ¥980 | 1 | 1 |
| LINE管理プロ | ¥4,980 | 1 | 3 |
| Starter | ¥4,980 | 1 | 3 |
| Pro | ¥9,800 | 2 | 10 |
| Growth OS | ¥24,800 | 5 | 無制限 |
| Enterprise | 要見積 | 100 | 無制限 |

詳細: [`buzzit/docs/BILLING-RULES.md`](./buzzit/docs/BILLING-RULES.md)

### 9.2 RunWith — 会社単位 + 席課金

```
月額 = プラン基本料 + max(0, 人数 - 2) × ¥980 + 医療モード（任意 +¥3,000）
```

| プラン | 基本料 | 含む人数 |
|--------|--------|---------|
| Free | ¥0 | 2人まで |
| Growth | ¥2,980 | 2人まで |
| Pro | ¥9,800 | 2人まで |

詳細: [`runwith/docs/BILLING-RULES.md`](./runwith/docs/BILLING-RULES.md)

> **Stripe 連携は未実装。** 現状は管理画面からプラン変更（Firestore 直接更新）。

---

## 10. 外部サービス連携

| サービス | 用途 | プロダクト |
|---------|------|-----------|
| **Google Gemini API** | AI 台本・Repurpose・音声→テキスト | BuzzIt API |
| **Meta Graph API** | Instagram / Facebook / Threads 投稿 | BuzzIt |
| **LINE Messaging API** | ブロードキャスト・セグメント・Webhook | BuzzIt |
| **Slack** | Webhook 通知・ネタ会議 | BuzzIt |
| **Ayrshare** | SNS 投稿（レガシー・任意） | BuzzIt |
| **Google Analytics 4** | LP・コーポレート計測 | 静的サイト |
| **Formspree** | お問い合わせ（プレースホルダー） | コーポレート |
| **Google Business Profile** | 自動投稿（Phase 4 予定） | BuzzIt |
| **Hot Pepper Beauty** | 予約トラッキング（Phase 5） | BuzzIt |

API キーは Firebase **Secret Manager** で管理（`GEMINI_API_KEY`, `AYRSHARE_API_KEY`, `SLACK_SIGNING_SECRET` 等）。

---

## 11. ビルド・デプロイ・CI/CD

### 11.1 統合ビルド

```bash
cd deploy
npm install
npm run build    # 全 5 プロジェクトをビルド → deploy/dist/
```

`build.mjs` の処理順:

1. `corporate-site` → `dist/web`
2. `runwith/landing-page` → `dist/web/runwith`
3. `buzzit/landing-page` → `dist/web/buzzit`
4. `runwith/app` → `dist/runwith-app`
5. `buzzit/app` → `dist/buzzit-app`
6. `robots.txt` / `sitemap.xml` 生成

### 11.2 デプロイコマンド

```bash
cd deploy

npm run deploy:web          # コーポレート + LP のみ（shigotoku-prod）
npm run deploy:buzzit-app   # BuzzIt アプリのみ
npm run deploy:buzzit-api   # BuzzIt API + Firestore + Storage
npm run deploy:runwith-app  # RunWith アプリのみ
npm run deploy:buzzit       # BuzzIt 一式（hosting + API + rules）
npm run deploy:runwith      # RunWith 一式
npm run deploy:all-with-api # 両プロジェクト全部（CI と同じ）
```

### 11.3 CI/CD（GitHub Actions）

| 項目 | 内容 |
|------|------|
| ワークフロー | `.github/workflows/deploy.yml` |
| トリガー | `main` への push、`workflow_dispatch` |
| Node | 22 |
| コマンド | `npm run deploy:all-with-api --prefix deploy` |
| Secrets | `FIREBASE_TOKEN`（必須）, `PUBLIC_GA_MEASUREMENT_ID`（任意） |

---

## 12. 環境変数

### 12.1 命名規則

| プレフィックス | 対象 | 注入タイミング |
|---------------|------|---------------|
| `PUBLIC_*` | Astro（LP・コーポレート） | ビルド時にインライン |
| `VITE_*` | Vite / React アプリ | ビルド時にインライン |
| Secret Manager | BuzzIt API キー | ランタイム（Functions） |

本番の Firebase Web 設定は `deploy/build.config.mjs` に定義され、`npm run build` 時に各プロジェクトへ注入されます。

### 12.2 各プロジェクトの .env.example

| パス | 主な変数 |
|------|---------|
| `corporate-site/.env.example` | `PUBLIC_RUNWITH_*`, `PUBLIC_BUZZIT_*`, GA |
| `runwith/landing-page/.env.example` | `PUBLIC_APP_URL`, GA |
| `runwith/app/.env.example` | `VITE_FIREBASE_*`, `VITE_LANDING_URL` |
| `buzzit/landing-page/.env.example` | `PUBLIC_APP_URL`, `PUBLIC_CORPORATE_URL`, GA |
| `buzzit/app/.env.example` | `VITE_FIREBASE_*`, `VITE_API_URL` |
| `buzzit/api/.env.example` | `GEMINI_API_KEY`, `AYRSHARE_API_KEY`, `BUZZIT_APP_ORIGIN` 等 |

---

## 13. リポジトリ構成

```
shigotoku/
├── シゴトク_プロダクト全体仕様書.md   ← 本書
├── README.md                          ← クイックスタート
├── corporate-site/                    ← コーポレート（Astro）
├── runwith/
│   ├── landing-page/                  ← ランウィズ LP（Astro）
│   ├── app/                           ← ランウィズ アプリ（React）
│   ├── supabase/                      ← 参考用マイグレーション（本番未使用）
│   └── docs/                          ← RunWith ドキュメント
├── buzzit/
│   ├── landing-page/                  ← BuzzIt LP（Astro）
│   ├── app/                           ← BuzzIt アプリ（React）
│   ├── api/                           ← BuzzIt API（Cloud Functions）
│   └── docs/                          ← BuzzIt ドキュメント
├── deploy/                            ← 統合ビルド・Firebase 設定
├── docs/                              ← 横断ドキュメント（デプロイ・SEO・CI）
└── .github/workflows/deploy.yml       ← CI/CD
```

---

## 14. 関連ドキュメント索引

### 横断（`docs/`）

| ファイル | 内容 |
|---------|------|
| [`docs/GCP-DEPLOY.md`](./docs/GCP-DEPLOY.md) | Firebase デプロイ手順・Secret 設定 |
| [`docs/FIREBASE-SPLIT.md`](./docs/FIREBASE-SPLIT.md) | 2 プロジェクト分割の理由と手順 |
| [`docs/DEPLOY-STATUS.md`](./docs/DEPLOY-STATUS.md) | 本番反映状況チェックリスト |
| [`docs/GITHUB-SETUP.md`](./docs/GITHUB-SETUP.md) | GitHub Actions 設定 |
| [`docs/SEO-MARKETING.md`](./docs/SEO-MARKETING.md) | sitemap・OGP・GA4 |

### BuzzIt（`buzzit/docs/`）

| ファイル | 内容 |
|---------|------|
| [`buzzit/docs/BuzzIt 要件定義書.md`](./buzzit/docs/BuzzIt%20要件定義書.md) | プロダクト要件・技術アーキテクチャ v5 |
| [`buzzit/docs/PRICING-STRATEGY.md`](./buzzit/docs/PRICING-STRATEGY.md) | 価格戦略・競合比較 |
| [`buzzit/docs/BILLING-RULES.md`](./buzzit/docs/BILLING-RULES.md) | 店舗課金ルール |
| [`buzzit/docs/COMPETITIVE-ROADMAP.md`](./buzzit/docs/COMPETITIVE-ROADMAP.md) | 開発ロードマップ |
| [`buzzit/docs/LSTEP-REPLACEMENT.md`](./buzzit/docs/LSTEP-REPLACEMENT.md) | Lステップ代替戦略 |
| [`buzzit/docs/競合アプリの機能・費用調査.md`](./buzzit/docs/競合アプリの機能・費用調査.md) | 競合調査 |

### RunWith（`runwith/docs/`）

| ファイル | 内容 |
|---------|------|
| [`runwith/docs/BILLING-RULES.md`](./runwith/docs/BILLING-RULES.md) | 会社・席課金ルール |

---

## 付録: アーキテクチャ概略図

```
                    ┌─────────────────────────────────────┐
                    │     Firebase Hosting (shigotoku-prod) │
                    │  shigotoku.com                        │
                    │  ├── /           corporate-site (Astro)│
                    │  ├── /runwith/   RunWith LP (Astro)    │
                    │  └── /buzzit/    BuzzIt LP (Astro)     │
                    └─────────────────────────────────────┘

┌──────────────────────────────┐    ┌──────────────────────────────┐
│ app.buzzit.shigotoku.com     │    │ app.runwith.shigotoku.com    │
│ BuzzIt App (React + Vite)    │    │ RunWith App (React + Vite)   │
│         │                    │    │         │                    │
│    /api/** ──rewrite──┐      │    │         │                    │
└───────────────────────│──────┘    └─────────│────────────────────┘
                        │                       │
                        ▼                       ▼
              ┌─────────────────┐    ┌─────────────────────────┐
              │ Cloud Functions │    │ Firestore (runwith-prod)│
              │ buzzitApi       │    │ Firebase Auth           │
              │ (Express/Node22)│    │ Firebase Storage        │
              └────────┬────────┘    └─────────────────────────┘
                       │
                       ▼
              ┌─────────────────┐
              │ Firestore       │
              │ (shigotoku-prod)│
              │ Storage / Secrets│
              └─────────────────┘
                       │
                       ▼
              ┌─────────────────┐
              │ 外部 API         │
              │ Gemini / Meta /  │
              │ LINE / Slack     │
              └─────────────────┘
```

---

*本書はリポジトリ内のソース・設定ファイルに基づいて作成されています。詳細な機能要件は各プロダクトの個別ドキュメントを参照してください。*
