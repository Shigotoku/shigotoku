import PageHeader from "../components/PageHeader";
import StubPanel from "../components/StubPanel";

export default function TeamPage() {
  return (
    <>
      <PageHeader title="スタッフ" description="スタッフ招待・未読者一覧・既読確認の管理。" />
      <StubPanel>スタッフ招待・未読者一覧は Phase 2 / Phase 3 で実装します。要件定義書 §8.4 を参照。</StubPanel>
    </>
  );
}
