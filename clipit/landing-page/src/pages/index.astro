---
import Layout from '../layouts/Layout.astro';
import HeroSection from '../components/HeroSection.astro';
import TrustBar from '../components/TrustBar.astro';
import ProblemSection from '../components/ProblemSection.astro';
import HowItWorksSection from '../components/HowItWorksSection.astro';
import CreateMethodsSection from '../components/CreateMethodsSection.astro';
import BulkUpdateSection from '../components/BulkUpdateSection.astro';
import PricingPreviewSection from '../components/PricingPreviewSection.astro';
import FaqSection from '../components/FaqSection.astro';
import { appPath, sitePath } from '../lib/urls';

const loginUrl = appPath('/login');
const pricingUrl = sitePath('pricing');

const features = [
  {
    title: '操作を記録するだけ',
    body: 'Chrome拡張でクリックごとにスクショと手順を自動取得。赤枠・番号付きで「どこを押すか」が一目です。',
  },
  {
    title: 'AIが日本語の手順文を生成',
    body: 'かんたん・丁寧・業務マニュアル風。新人向け・患者向けなど、読む人に合わせた文案に整えます。',
  },
  {
    title: 'まとめて修正で古くならない',
    body: 'システム名や連絡先が変わっても、全マニュアルを横断検索してAIが変更候補を提案。確認してから一括適用。',
  },
  {
    title: '印刷・QR・スマホで現場に届く',
    body: 'A4印刷、QRコード、スマホ閲覧。個人情報はマスキングし、公開前に隠し忘れ確認を通します。',
  },
];
---

<Layout title="クリッピット (ClipIt) - 操作するだけで、業務マニュアルが完成">
  <HeroSection />
  <TrustBar />
  <ProblemSection />
  <HowItWorksSection />
  <CreateMethodsSection />
  <BulkUpdateSection />

  <section id="features" class="clipit-section mx-auto max-w-7xl px-4 py-10 sm:px-6 sm:py-14">
    <div class="text-center">
      <h2 class="clipit-h2 text-2xl font-bold tracking-tight text-slate-900 sm:text-3xl" data-jp-heading>ClipIt の4つの特徴</h2>
      <p class="clipit-lead mt-3 text-base text-slate-600" data-jp-wrap>作る・直す・届ける。マニュアル管理に必要なことだけ、シンプルに。</p>
    </div>
    <div class="clipit-grid-safe mt-8 grid gap-4 sm:grid-cols-2 sm:gap-5">
      {features.map((f) => (
        <div class="min-w-0 rounded-2xl border border-slate-200 bg-white p-5 sm:p-6">
          <h3 class="clipit-h3 text-base font-bold text-slate-900 sm:text-lg" data-jp-heading>{f.title}</h3>
          <p class="clipit-body mt-2 text-sm leading-relaxed text-slate-600" data-jp-wrap>{f.body}</p>
        </div>
      ))}
    </div>
  </section>

  <PricingPreviewSection />
  <FaqSection />

  <section class="clipit-section--lg relative overflow-hidden bg-slate-950 py-14 text-center sm:py-20">
    <div class="clipit-cta-glow pointer-events-none absolute inset-0" aria-hidden="true"></div>
    <div class="relative mx-auto max-w-3xl px-4 sm:px-6">
      <h2 class="clipit-h2 text-2xl font-bold tracking-tight text-white sm:text-3xl" data-jp-heading>まずは1本、作ってみてください。</h2>
      <p class="clipit-lead mt-3 text-base text-slate-400 sm:text-lg" data-jp-wrap>Googleログインで登録。クレジットカード不要。最短10分で初回マニュアルが完成します。</p>
      <div class="mt-8 flex flex-col items-stretch justify-center gap-3 sm:flex-row sm:items-center">
        <a href={loginUrl} class="inline-flex justify-center rounded-xl bg-primary-500 px-8 py-3.5 text-base font-bold text-white transition-all hover:bg-primary-400">
          無料で始める
        </a>
        <a href={pricingUrl} class="inline-flex justify-center rounded-xl border border-white/20 px-8 py-3.5 text-base font-semibold text-slate-200 hover:border-white/40 hover:text-white">
          料金プランの詳細
        </a>
      </div>
    </div>
  </section>
</Layout>

<style>
  .clipit-cta-glow {
    background: radial-gradient(ellipse 70% 60% at 50% 100%, rgba(249, 115, 22, 0.25), transparent);
  }
</style>
